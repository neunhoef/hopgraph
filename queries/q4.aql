FOR a IN A
  LET x = (FOR b IN 1..1 OUTBOUND a._id GRAPH "hopgraph"
             PRUNE IS_SAME_COLLECTION(b, "B1")
             FOR c IN 1..1 OUTBOUND b._id GRAPH "hopgraph"
               FILTER IS_SAME_COLLECTION(c, "C1")
               LIMIT 1
               RETURN {b, c})
  FILTER LENGTH(x) > 0
  LIMIT 1000
  RETURN {a, b: x[0].b, c: x[0].c}

